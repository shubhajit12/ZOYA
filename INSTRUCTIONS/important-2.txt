APPROVED — START PHASE 2 ONLY.

Proceed with the Phase 2 plan exactly as described in your audit:

1. Determine the exact @pixiv/three-vrm 3.x version compatible with the project's existing three@0.185.1.
2. Pin that exact version.
3. Add the isolated Carlotta VRM loader:
   src/mint/carlottaVrmLoader.ts
4. Use GLTFLoader + the appropriate three-vrm VRM0 loading/plugin path.
5. Do NOT replace Mint's loader.
6. Do NOT change Mint's current rendering/lip-sync/animation implementation.
7. Do NOT change Groq.
8. Do NOT change TTS.
9. Do NOT change camera, controls, UI, memory, browser, screen sharing, or PC control.
10. Do NOT modify the Carlotta .vrm asset.
11. Do NOT add Mixamo or any external animation files.

IMPORTANT:
Keep the new VRM loader isolated and side-by-side with the existing FBX/Mint loader.

After the dependency and loader changes:

* run TypeScript/static validation
* run npm run lint
* run npm run build
* verify the loader imports successfully
* verify the VRM0 plugin path is valid
* verify there are no regressions in the existing Mint build

DO NOT proceed to Phase 3 yet.

STOP after Phase 2 validation and give me:

1. Exact three-vrm version selected
2. Why it is compatible with three@0.185.1
3. Files changed
4. Files added
5. npm/package changes
6. Validation results
7. Lint result
8. Build result
9. Any warnings/errors
10. Confirmation that Mint's existing runtime remains intact

Only after reporting this will we approve Phase 3.
